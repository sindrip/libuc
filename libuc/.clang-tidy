Checks: >
  -*,
  readability-braces-around-statements
WarningsAsErrors: '*'
# Everything not ours — the uapi export, clang's own headers — arrives through
# -isystem and is excluded before this regex is consulted, so '.*' means
# "libuc's headers, however the compile command spells the path".
HeaderFilterRegex: '.*'
